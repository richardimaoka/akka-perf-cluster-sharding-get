actor-system-name = "RichardPerf"

akka {
  actor {
    provider = cluster
  }

  remote {
    log-remote-lifecycle-events = off
    netty.tcp {
      hostname = "127.0.0.1"
      port = 0
    }
  }

  cluster.seed-nodes = [
    "akka.tcp://"${actor-system-name}"@127.0.0.1:2551"
  ]
}
